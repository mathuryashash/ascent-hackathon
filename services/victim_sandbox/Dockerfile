# This top-level Dockerfile is superseded by the per-service Dockerfiles:
#
#   services/victim_sandbox/victim/Dockerfile   — vulnerable Flask app
#   services/victim_sandbox/sandbox/Dockerfile  — isolated payload executor
#   services/victim_sandbox/siem/Dockerfile     — log monitor / SIEM
#
# Each service is built individually by docker-compose.yml at the project root.
# This file is kept as a reference stub only.

FROM python:3.11-slim
RUN echo "Use the per-service Dockerfiles via docker compose up"
